import { SDKInitConfig, AdConfig } from "../../types";
export declare class SDKClient {
    private _adFactory;
    private _clientIdentity;
    private _environment;
    constructor(config: SDKInitConfig);
    showBannerAd: ({ adUnitId, containerId, adConfig }: {
        adUnitId: string;
        containerId: string;
        adConfig: AdConfig;
    }, cb?: ((error: string) => void) | undefined) => Promise<void>;
    setWalletAddress(walletAddress: string): void;
    setUserEmail(userEmail: string): void;
}
